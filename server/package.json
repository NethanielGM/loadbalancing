{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build:1": "docker compose up --scale api=1",
    "build:2": "docker compose up --scale api=2",
    "build:3": "docker compose up --scale api=3",
    "build:4": "docker compose up --scale api=4",
    "build:5": "docker compose up --scale api=5"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "address": "^1.2.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "nodemon": "^2.0.19",
    "public-ip": "^6.0.1"
  }
}
